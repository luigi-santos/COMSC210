// Lab 32 || Luigi Santos

#include <iostream>
#include <cstdlib>
#include <ctime>
#include "Car.h"

using namespace std;

int main() {
  srand(static_cast<unsigned int>(time(nullptr)));

  deque<Car> tollQueue;

  for (int i = 0; i < INITIAL_QUEUE_SIZE; ++i) {
    tollQueue.push_back(Car());
  }

  cout << "Initial queue: " << endl;
  for (const auto& car : tollQueue) {
    car.print();
  }

  int timeStep = 0;

  while (!tollQueue.empty()) {
    ++timeStep;

    int randomEvent = rand() % 100;

    if (randomEvent < 55) {
      Car departingCar = tollQue.front();
      tollQueue.pop_front();
      cout << endl;
      cout << "Time: " << timeStep << " Operation: Car paid: ";
      departingCar.print();
    }
